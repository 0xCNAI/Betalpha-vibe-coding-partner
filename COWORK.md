# Cowork Instructions

Use `AGENTS.md` for trigger timing. Detailed Betavibe workflow lives in the installed `betavibe-insight` skill; load it for non-trivial coding/spec/debug work. If skills are unavailable, search `registry/insights/**/INSIGHT.md` directly and never save unverified or generic notes as reviewed insights.

# Cowork Instructions

Use the markdown registry and CLI exactly as described in `AGENTS.md`. If shell access is unavailable, read `registry/insights/**/INSIGHT.md` directly and search by tags / prevention signals.
